import { {{domainNamePascal}}ListItemModel } from '../../../application/query/view/{{domainNameKebab}}-list-item.model';

export class Find{{domainNamePascal}}ListResponseDto {
  readonly {{domainNameCamel}}List: {{domainNameCamel}}ListItemDto[];

  private constructor({{domainNameCamel}}List: {{domainNameCamel}}ListItemDto[]) {
    this.{{domainNameCamel}}List = {{domainNameCamel}}List;
  }

  static from(modelList: {{domainNamePascal}}ListItemModel[]): Find{{domainNamePascal}}ListResponseDto {
    const {{domainNameCamel}}List = modelList.map({{domainNameCamel}}ListItemDto.from);
    return new Find{{domainNamePascal}}ListResponseDto({{domainNameCamel}}List);
  }
}

class {{domainNameCamel}}ListItemDto {
  readonly idx: number;
  readonly required: string;
  readonly nullable: string | null;
  readonly updatedAt: Date;
  readonly createdAt: Date;

  private constructor(model: {{domainNamePascal}}ListItemModel) {
    this.idx = model.idx;
    this.required = model.required;
    this.nullable = model.nullable;
    this.updatedAt = model.updatedAt;
    this.createdAt = model.createdAt;
  }

  static from(model: {{domainNamePascal}}ListItemModel): {{domainNameCamel}}ListItemDto {
    return new {{domainNameCamel}}ListItemDto(model);
  }
}
