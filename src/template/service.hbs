import { Injectable, NotFoundException } from '@nestjs/common';
import { {{domainNamePascal}}Repository } from '../infrastructure/{{domainNameKebab}}.repository';
import { {{domainNamePascal}}Domain } from './{{domainNameKebab}}.domain';
import { {{domainNamePascal}}CreateProps, {{domainNamePascal}}UpdateProps } from './{{domainNameKebab}}.type';

@Injectable()
export class {{serviceClassName}} {
  constructor(private readonly {{domainNameCamel}}Repository: {{domainNamePascal}}Repository) {}

  async create{{domainNamePascal}}(props: {{domainNamePascal}}CreateProps): Promise<void> {
    await this.{{domainNameCamel}}Repository.create{{domainNamePascal}}(props);
  }

  async update{{domainNamePascal}}(idx: number, props: {{domainNamePascal}}UpdateProps): Promise<void> {
    await this.{{domainNameCamel}}Repository.update{{domainNamePascal}}(idx, props);
  }

  async delete{{domainNamePascal}}(idx: number): Promise<void> {
    await this.{{domainNameCamel}}Repository.delete{{domainNamePascal}}(idx);
  }

  async find{{domainNamePascal}}ByIdx(idx: number): Promise<{{domainNamePascal}}Domain> {
    const {{domainNameCamel}} = await this.{{domainNameCamel}}Repository.find{{domainNamePascal}}ByIdx(idx);
    if (!{{domainNameCamel}}) throw new NotFoundException('{{domainNameWords}} not found');
    return {{domainNameCamel}};
  }

  async find{{domainNamePascal}}List(): Promise<{{domainNamePascal}}Domain[]> {
    return await this.{{domainNameCamel}}Repository.find{{domainNamePascal}}List();
  }
}
