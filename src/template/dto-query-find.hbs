import { {{domainNamePascal}}DetailModel } from '../../../application/query/view/{{domainNameKebab}}-detail.model';

export class Find{{domainNamePascal}}ResponseDto {
  readonly required: string;
  readonly nullable: string | null;
  readonly createdAt: Date;

  private constructor(
    private readonly model: {
      required: string;
      nullable: string | null;
      createdAt: Date;
    },
  ) {
    this.required = model.required;
    this.nullable = model.nullable;
    this.createdAt = model.createdAt;
  }

  static from(model: {{domainNamePascal}}DetailModel): Find{{domainNamePascal}}ResponseDto {
    return new Find{{domainNamePascal}}ResponseDto({
      required: model.required,
      nullable: model.nullable,
      createdAt: model.createdAt,
    });
  }
}
