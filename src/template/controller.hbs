import { Controller, HttpCode, HttpStatus, Get, Param, ParseIntPipe } from '@nestjs/common';
import { Find{{domainNamePascal}}UseCase } from '../application/find-{{domainNameKebab}}.use-case';

@Controller('{{domainNamePluralKebab}}')
export class {{controllerClassName}} {
  constructor(private readonly find{{domainNamePascal}}UseCase: Find{{domainNamePascal}}UseCase) {}

  @HttpCode(HttpStatus.OK)
  @Get('/:{{domainNameCamel}}Id')
  async find{{domainNamePascal}}(@Param('{{domainNameCamel}}Id', ParseIntPipe) idx: number): Promise<any> {
    return await this.find{{domainNamePascal}}UseCase.execute(idx);
  }
}
